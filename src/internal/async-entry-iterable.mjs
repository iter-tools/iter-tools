/**
 * @generated-from ./$entry-iterable.js
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { AsyncResultIterable } from './async-iterable';
export function AsyncEntryIterable(...args) {
  AsyncResultIterable.call(this, ...args);
}

async function* keys(iterable) {
  for await (const [key] of iterable) yield key;
}

async function* values(iterable) {
  for await (const [, value] of iterable) yield value;
}

AsyncEntryIterable.prototype = Object.assign(Object.create(AsyncResultIterable.prototype), {
  constructor: AsyncEntryIterable,

  keys() {
    return new AsyncResultIterable(keys, [this]);
  },

  values() {
    return new AsyncResultIterable(values, [this]);
  },

  entries() {
    return this;
  },
});
