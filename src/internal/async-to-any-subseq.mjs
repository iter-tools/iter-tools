/**
 * @generated-from ./$to-any-subseq.js
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { toArray as syncToArray } from '../methods/$to-array/to-array';
import { asyncToArray } from '../methods/$to-array/async-to-array';
import { asyncMap } from '../methods/$map/async-map';

async function toAnySubseq({ any, subseq }, value) {
  const values = any ? syncToArray(value) : [value];
  const subseqs = await asyncToArray(
    asyncMap(values, value =>
      subseq ? (typeof value === 'string' ? value : asyncToArray(value)) : [value],
    ),
  );
  return subseqs;
}

export default toAnySubseq;
