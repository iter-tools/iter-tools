/* @macrome
 * @generatedby /generate/generators/impls/index.cjs
 * @generatedfrom ./$join-with-seq.test.ts#1643907550748
 * This file is autogenerated. Please do not edit it directly.
 * When editing run `npx macrome watch` then change the file this is generated from.
 */
import { asyncJoinWithSeq } from 'iter-tools-es';
import { asyncWrapDeep, asyncUnwrap } from '../../../test/async-helpers.js';

describe('asyncJoinWithSeq', () => {
  describe('joining on the empty seq', () => {
    it('should include the values from every group', async () => {
      expect(await asyncUnwrap(asyncJoinWithSeq([], asyncWrapDeep([[1], [2], [3]])))).toEqual([
        1,
        2,
        3,
      ]);
    });

    it('should contain no output for an empty group', async () => {
      expect(await asyncUnwrap(asyncJoinWithSeq([], asyncWrapDeep([[1], [], [3]])))).toEqual([
        1,
        3,
      ]);
    });
  });

  it('should yield a single separator when joining two empty groups', async () => {
    expect(await asyncUnwrap(asyncJoinWithSeq([1, 2], asyncWrapDeep([[], []])))).toEqual([1, 2]);
  });

  it('passes through the empty iterable', async () => {
    expect(await asyncUnwrap(asyncJoinWithSeq([], null))).toEqual([]);
    expect(await asyncUnwrap(asyncJoinWithSeq([], undefined))).toEqual([]);
    expect(await asyncUnwrap(asyncJoinWithSeq([], asyncWrapDeep([])))).toEqual([]);
  });
});
