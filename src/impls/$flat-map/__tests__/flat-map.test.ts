/* @macrome
 * @generatedby /generate/generators/impls/index.cjs
 * @generatedfrom ./$flat-map.test.ts#1643837503065
 * This file is autogenerated. Please do not edit it directly.
 * When editing run `npx macrome watch` then change the file this is generated from.
 */
import { flatMap } from 'iter-tools-es';
import { wrap, unwrap } from '../../../test/helpers.js';

describe('flatMap', () => {
  describe('when source is empty', () => {
    it('yields no values', () => {
      expect(unwrap(flatMap((value: any) => value, null))).toEqual([]);
      expect(unwrap(flatMap((value: any) => value, undefined))).toEqual([]);
      expect(unwrap(flatMap((value: any) => value, wrap([])))).toEqual([]);
    });
  });

  describe('when source has values', () => {
    it('concatenates result of func(value, i) for each value in source', () => {
      const iter = flatMap((value: number, i: number) => [i, value], wrap([1, 2, 3]));
      expect(unwrap(iter)).toEqual([0, 1, 1, 2, 2, 3]);
    });
  });
});
