/**
 * @generated-from ./$split-groups-by.test.ts
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { splitGroupsBy } from 'iter-tools-es';
import { wrap, unwrap, unwrapDeep } from '../../../test/helpers.js';

function identity<T>(value: T): T {
  return value;
}

describe('splitGroupsBy', () => {
  describe('when source is empty', () => {
    it('yields no groups', () => {
      expect(unwrapDeep(splitGroupsBy(identity, null))).toEqual([]);
      expect(unwrapDeep(splitGroupsBy(identity, undefined))).toEqual([]);
      expect(unwrapDeep(splitGroupsBy(identity, wrap([])))).toEqual([]);
    });
  });

  describe('when values from source cannot be grouped', () => {
    it('yields a group for each value', () => {
      expect(unwrapDeep(splitGroupsBy((_: number, i: number) => i, wrap([0, 0, 0])))).toEqual([
        [0, [0]],
        [1, [0]],
        [2, [0]],
      ]);
    });
  });

  describe('when source contains subsequent values belonging to the same group', () => {
    it('coalesces values into groups', () => {
      const lowerCase = (value: string) => value.toLowerCase();
      expect(unwrapDeep(splitGroupsBy(lowerCase, 'AaA'))).toEqual([['a', ['A', 'a', 'A']]]);
      expect(unwrapDeep(splitGroupsBy(lowerCase, 'baA'))).toEqual([
        ['b', ['b']],
        ['a', ['a', 'A']],
      ]);
    });
  });

  describe('when groups are consumed out of order', () => {
    it('throws', () => {
      const iter = splitGroupsBy(identity, 'AB');
      const [, As] = iter.next().value;
      const [, Bs] = iter.next().value;

      unwrap(Bs);

      expect(
        (() => {
          try {
            unwrap(As);
          } catch (e) {
            return e;
          }
        })(),
      ).toMatchSnapshot();
    });
  });
});
