/**
 * @generated-from ./$min-by.test.ts
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { minBy } from 'iter-tools-es';
import { wrap } from '../../../test/helpers.js';

type Person = { name: string; age: number };
const personAge = (p: Person) => p.age;
const ascendingOrder = (a: number, b: number) => a - b;
const descendingOrder = (a: number, b: number) => b - a;

const youngest = {
  name: 'a',
  age: 10,
};
const middle = {
  name: 'b',
  age: 20,
};
const oldest = {
  name: 'c',
  age: 30,
};

const people = [youngest, middle, oldest];

describe('minBy', () => {
  describe('when source is empty', () => {
    it('should return undefined', () => {
      expect(minBy(personAge, null)).toEqual(undefined);
      expect(minBy(personAge, ascendingOrder, null)).toEqual(undefined);
      expect(minBy(personAge, ascendingOrder, undefined)).toEqual(undefined);
      expect(minBy(personAge, ascendingOrder, wrap([]))).toEqual(undefined);
    });
  });

  describe('when source has a single value', () => {
    it('should return that value', () => {
      expect(minBy((i) => i, wrap([1]))).toEqual(1);
      expect(minBy((i) => i, ascendingOrder, wrap([1]))).toEqual(1);
      expect(minBy((i) => i, descendingOrder, wrap([1]))).toEqual(1);
    });
  });

  describe('when source has values', () => {
    it('should return the minimum value according mapper and selector function', () => {
      expect(minBy(personAge, wrap(people))).toEqual(youngest);
      expect(minBy(personAge, ascendingOrder, wrap(people))).toEqual(youngest);
      expect(minBy(personAge, descendingOrder, wrap(people))).toEqual(oldest);
    });
  });
});
