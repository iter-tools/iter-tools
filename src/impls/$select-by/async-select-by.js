/**
 * @generated-from ./$select-by.js
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { asyncIterableCurry } from '../../internal/async-iterable.js';
import { __asyncPeekerate } from '../$peekerate/async-peekerate.js';

export async function __asyncSelectBy(iterable, selector, mapper) {
  const peekr = await __asyncPeekerate(iterable);

  if (!peekr.done) {
    let bestItem = peekr.value;
    let bestValue = mapper(bestItem);

    await peekr.advance();
    while (!peekr.done) {
      const candidate = peekr.value;
      const candidateValue = mapper(candidate);
      if (selector(bestValue, candidateValue)) {
        bestItem = candidate;
        bestValue = candidateValue;
      }
      await peekr.advance();
    }
    return bestItem;
  }
}

export const asyncSelectBy = asyncIterableCurry(__asyncSelectBy, {
  reduces: true,
});
