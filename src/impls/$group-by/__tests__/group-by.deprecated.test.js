/**
 * @generated-from ./$group-by.deprecated.test.js
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { groupBy } from '@iter-tools/es';
import { unwrapDeep } from '../../../test/helpers.js';

describe('groupBy (deprecated)', () => {
  it('returns source values grouped by identity', () => {
    const source = 'ABBAAC';
    const result = [
      ['A', ['A']],
      ['B', ['B', 'B']],
      ['A', ['A', 'A']],
      ['C', ['C']],
    ];
    expect(unwrapDeep(groupBy(null, source))).toEqual(result);
    expect(unwrapDeep(groupBy(null, source))).toEqual(result);
    expect(console.warn).callsMatchSnapshot();
  });
});
