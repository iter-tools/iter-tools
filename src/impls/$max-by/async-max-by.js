/**
 * @generated-from ./$max-by.js
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { asyncIterableCurry } from '../../internal/async-iterable.js';
import { __asyncPeekerate } from '../$peekerate/async-peekerate.js';
import { defaultCompareOrder } from '../../internal/compare.js';

export async function __asyncMaxBy(iterable, mapper, compare = defaultCompareOrder) {
  const peekr = await __asyncPeekerate(iterable);

  if (!peekr.done) {
    let maxValue = peekr.value;
    let maxMappedValue = await mapper(maxValue);

    await peekr.advance();
    while (!peekr.done) {
      const value = peekr.value;
      const mappedValue = await mapper(value);
      if (compare(maxMappedValue, mappedValue) < 0) {
        maxValue = value;
        maxMappedValue = mappedValue;
      }
      await peekr.advance();
    }
    return maxValue;
  }
}

export const asyncMaxBy = asyncIterableCurry(__asyncMaxBy, {
  reduces: true,
  minArgs: 1,
  maxArgs: 2,
  growRight: true,
});
