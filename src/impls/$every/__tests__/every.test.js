/**
 * @generated-from ./$every.test.js
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { every } from 'iter-tools-es';
import { wrap } from '../../../test/helpers.js';

describe('every', () => {
  describe('when iterable is empty', () => {
    it('returns true', () => {
      expect(every(() => true, null)).toEqual(true);
      expect(every(() => true, undefined)).toEqual(true);
      expect(every(() => true, wrap([]))).toEqual(true);
    });
  });

  describe('when no values match predicate', () => {
    it('returns false', () => {
      expect(every((val) => val !== val, [1, 2, 3])).toBe(false);
    });
  });

  describe('when some values match predicate', () => {
    it('returns false', () => {
      expect(every((val) => val > 2, [1, 2, 3])).toBe(false);
    });
  });

  describe('when every value matches predicate', () => {
    it('returns true', () => {
      expect(every((val) => val > 0, [1, 2, 3])).toBe(true);
    });
  });
});
