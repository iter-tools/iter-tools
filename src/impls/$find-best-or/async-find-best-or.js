/**
 * @generated-from ./$find-best-or.js
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

import { asyncIterableCurry } from '../../internal/async-iterable.js';
import { __asyncPeekerate } from '../$peekerate/async-peekerate.js';

export async function __asyncFindBestOr(
  iterable,
  notFoundValue,
  comparer,
  mapper = (value) => value,
) {
  const peekr = await __asyncPeekerate(iterable);

  if (peekr.done) {
    return notFoundValue;
  } else {
    let best = peekr.value;
    let bestValue = await mapper(best, peekr.index);

    await peekr.advance();

    while (!peekr.done) {
      const candidate = peekr.value;
      const candidateValue = await mapper(candidate, peekr.index);
      if (comparer(bestValue, candidateValue)) {
        best = candidate;
        bestValue = candidateValue;
      }

      await peekr.advance();
    }
    return best;
  }
}

export const asyncFindBestOr = asyncIterableCurry(__asyncFindBestOr, {
  reduces: true,
  minArgs: 2,
  maxArgs: 3,
  growRight: true,
});
