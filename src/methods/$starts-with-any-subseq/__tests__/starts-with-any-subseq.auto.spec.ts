/**
 * @generated-from ./starts-with-any-subseq.test.js
 * This file is autogenerated from a template. Please do not edit it directly.
 * To rebuild it from its template use the command
 * > npm run generate
 * More information can be found in CONTRIBUTING.md
 */

/* eslint-disable no-unused-vars,import/no-duplicates,no-constant-condition */

import { startsWithAnySubseq } from '../../..';
import { wrap } from '../../../test/helpers';

describe('startsWithAnySubseq', () => {
  describe('when no sequences are given', () => {
    it('returns false', () => {
      expect(startsWithAnySubseq([], wrap([]))).toBe(false);
    });
  });

  describe('when iterable starts with a given sequence', () => {
    it('returns true', () => {
      expect(startsWithAnySubseq([wrap([1, 2])], wrap([1, 2, 3]))).toBe(true);
      expect(startsWithAnySubseq([wrap([1, 2]), wrap([1, 2, 3])], wrap([1, 2, 3]))).toBe(true);
      expect(startsWithAnySubseq([wrap([1, 2, 3]), wrap([1, 2])], wrap([1, 2, 3]))).toBe(true);
    });
  });

  describe('when iterable is equal to a given sequence', () => {
    it('returns true', () => {
      expect(startsWithAnySubseq([wrap([1, 2, 3])], wrap([1, 2, 3]))).toBe(true);
    });
  });

  describe('when iterable is shorter than a matching sequence', () => {
    it('returns false', () => {
      expect(startsWithAnySubseq([wrap([1, 2, 3])], wrap([1, 2]))).toBe(false);
    });
  });

  describe('when iterable includes but does not start with a given sequence', () => {
    it('returns false', () => {
      expect(startsWithAnySubseq([wrap([2, 3])], wrap([1, 2, 3]))).toBe(false);
    });
  });

  describe('when iterable is empty', () => {
    describe('and any sequence is empty', () => {
      it('returns true', () => {
        expect(startsWithAnySubseq([wrap([]), wrap([null])], wrap([]))).toBe(true);
        expect(startsWithAnySubseq([null], wrap([]))).toBe(true);
      });
    });

    describe('and no sequence is empty', () => {
      it('returns false', () => {
        expect(startsWithAnySubseq([wrap([undefined])], wrap([]))).toBe(false);
      });
    });
  });
});
